{
  "mcpServers": {
    "memorable": {
      "type": "stdio",
      "command": "bash",
      "args": ["scripts/mcp-start.sh"],
      "env": {
        "ALLOW_HTTP_DEV": "true",
        "NODE_ENV": "development",
        "TRANSPORT_TYPE": "stdio",
        "MCP_USER_ID": "default"
      }
    }
  }
}
