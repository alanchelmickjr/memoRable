diff --git a/src/ctx_to_lora/modeling/text_to_lora.py b/src/ctx_to_lora/modeling/text_to_lora.py
index 6acb8be..2c875c5 100644
--- a/src/ctx_to_lora/modeling/text_to_lora.py
+++ b/src/ctx_to_lora/modeling/text_to_lora.py
@@ -15,10 +15,14 @@ from ctx_to_lora.utils import get_peft_modules
 
 
 class TextToLoRA(nn.Module):
-    def __init__(self, model_name_or_path, prefix_tokens, device):
-        assert model_name_or_path == "google/gemma-2-2b-it"
+    def __init__(self, model_name_or_path, prefix_tokens, device, load_in_4bit=True):
         super().__init__()
-        hypermod_dir = "trained_t2l/gemma_2b_t2l"
+        # Determine hypermod_dir from model name
+        if "9b" in model_name_or_path:
+            hypermod_dir = "trained_t2l/gemma_9b_t2l"
+        else:
+            hypermod_dir = "trained_t2l/gemma_2b_t2l"
+        
         peft_config = get_peft_config(
             PeftConfig.from_json_file(f"{hypermod_dir}/adapter_config.json")
         )
@@ -27,6 +31,7 @@ class TextToLoRA(nn.Module):
         peft_config.lora_alpha = peft_config.lora_alpha / peft_config.r
         self.prefix_tokens = prefix_tokens
         self.device = device
+        
         (
             _,
             self.t2l_model,
@@ -36,7 +41,7 @@ class TextToLoRA(nn.Module):
             self.emb_tokenizer,
             self.task_desc_format_fn,
             self.pooling_fn,
-        ) = load_hypermod(hypermod_dir, device)
+        ) = load_hypermod(hypermod_dir, device, load_in_4bit=load_in_4bit)
         layer_indices = range(len(get_layers(self.base_model)))
 
         self.layer_indices = torch.tensor(
diff --git a/src/ctx_to_lora/modeling/text_to_lora_impl.py b/src/ctx_to_lora/modeling/text_to_lora_impl.py
index 1991174..c8b237b 100644
--- a/src/ctx_to_lora/modeling/text_to_lora_impl.py
+++ b/src/ctx_to_lora/modeling/text_to_lora_impl.py
@@ -16,7 +16,7 @@ from peft.tuners._buffer_dict import BufferDict
 from peft.tuners.tuners_utils import BaseTunerLayer, check_target_module_exists
 from peft.utils import get_peft_model_state_dict
 from safetensors.torch import save_file
-from transformers import AutoModel, AutoModelForCausalLM, AutoTokenizer
+from transformers import AutoModel, AutoModelForCausalLM, AutoTokenizer, BitsAndBytesConfig
 from transformers.models.llama.modeling_llama import LlamaRotaryEmbedding
 
 
@@ -124,6 +124,7 @@ def get_model_and_tokenizer(
     tokenizer_kwargs=None,
     device="cuda:0",
     dtype=torch.bfloat16,
+    load_in_4bit=False,
 ):
     model = get_model(
         model_path,
@@ -134,6 +135,7 @@ def get_model_and_tokenizer(
         model_kwargs,
         device,
         dtype,
+        load_in_4bit=load_in_4bit,
     )
     tokenizer = get_tokenizer(model_path, tokenizer_kwargs, peft_config, train)
     return model, tokenizer
@@ -181,6 +183,7 @@ def get_model(
     model_kwargs=None,
     device="cuda:0",
     dtype=torch.bfloat16,
+    load_in_4bit=False,
 ):
     model_init_kwargs = dict(
         pretrained_model_name_or_path=model_path,
@@ -188,6 +191,14 @@ def get_model(
         torch_dtype=dtype,
         trust_remote_code=True,
     )
+
+    if load_in_4bit:
+        model_init_kwargs["quantization_config"] = BitsAndBytesConfig(
+            load_in_4bit=True,
+            bnb_4bit_quant_type="nf4",
+            bnb_4bit_compute_dtype=dtype,
+        )
+
     if model_kwargs is not None:
         model_init_kwargs.update(model_kwargs)
     if use_flash_attn:
@@ -199,9 +210,14 @@ def get_model(
     model = AutoModelForCausalLM.from_pretrained(**model_init_kwargs)
     if peft_config is not None:
         model = PeftModel(model, peft_config)
-    model.train(train)
-    for param in model.parameters():
-        param.requires_grad = requires_grad
+    
+    if not load_in_4bit:
+        model.train(train)
+        for param in model.parameters():
+            param.requires_grad = requires_grad
+    else:
+        model.eval()
+
     return model
 
 
@@ -1080,7 +1096,7 @@ def save_hypermod_checkpoint(save_dir, hypermod, curstep):
     return save_path
 
 
-def load_hypermod_checkpoint(checkpoint_path, device):
+def load_hypermod_checkpoint(checkpoint_path, device, load_in_4bit=False):
     base_hypermod_dir = os.path.dirname(checkpoint_path)
     if "checkpoint" in base_hypermod_dir:
         base_hypermod_dir = base_hypermod_dir.split("checkpoint")[0]
@@ -1099,6 +1115,7 @@ def load_hypermod_checkpoint(checkpoint_path, device):
         peft_config=peft_config,
         model_kwargs={"output_hidden_states": True, "output_attentions": False},
         device=device,
+        load_in_4bit=load_in_4bit,
     )
     # train to output delta_w for all layers
     layer_indices = torch.tensor(
@@ -1137,7 +1154,7 @@ def load_hypermod_checkpoint(checkpoint_path, device):
     )
 
 
-def load_hypermod(hypermod_dir, device):
+def load_hypermod(hypermod_dir, device, load_in_4bit=False):
     checkpoint_path = f"{hypermod_dir}/hypermod.pt"
     (
         args,
@@ -1148,7 +1165,7 @@ def load_hypermod(hypermod_dir, device):
         emb_tokenizer,
         task_desc_format_fn,
         pooling_fn,
-    ) = load_hypermod_checkpoint(checkpoint_path, device)
+    ) = load_hypermod_checkpoint(checkpoint_path, device, load_in_4bit=load_in_4bit)
     return (
         args,
         hypermod,
